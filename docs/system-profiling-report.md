# 系統 Profiling 報告：軟體與硬體耗時

## 2026-09-01 16:04 弧長最佳化後實飛

結論先行：中心線量測改用累積弧長與二分搜尋後，`uiToTickMs` mean 從 **18.14 ms** 降至 **2.72 ms**、P95 從 **50.53 ms** 降至 **7.21 ms**，分別改善 **85.0%**、**85.7%**。相機影格擷取至 DJI MSDK 呼叫返回延遲為 mean **70.69 ms**、P95 **104.34 ms**；一般分布改善，但單一 OpenCV／UI callback 疊加離群使 max 升至 **193.21 ms**。

資料來源為 Pixel 8 Pro 上 session `1788249055206-252880825291200` 的 `/tmp/flight-profile-optimized.tsv`、`/tmp/flight-log-optimized.txt` 與螢幕錄影 `/tmp/screen-20260901-160544.mp4`。

證據 SHA-256：TSV `23870a62f5073f78c84cd344d67d62c49ed0c2ebad6346ceda8c9553b9cfff3d`、Flight Log `43724a66dad5839a527c0c60f5f01af631a61ece83931cfad3553e0971c3ea83`、影片 `49065594d50b597beb0b08f0296dc3b34d48ab5688a03cc243295fe6fa09dd67`。

量測窗從 `tracking_start` 到 `tracking_stop`，共 **36.009 秒**。循跡模式為固定航向 `FIXED_HEADING`、目標速度 **1.35 m/s**、控制相位超前 **16°**、控制週期 **20 Hz**、Virtual Stick **40 Hz**。最後由操作者以畫面右搖桿接管停止。

```text
相機影格擷取
  │  1.22 ms  複製 RGBA 影格並排入 detector worker（queueMs）
  ▼
Detector worker 開始處理
  │ 12.87 ms  Mat、縮放、RGBA→RGB／灰階與模糊（preprocessMs）
  ▼
影像前處理完成
  │  0.92 ms  Otsu 門檻、黑色分割與明暗修正（thresholdMs）
  ▼
黑色分割完成
  │  3.38 ms  Lab 地板顏色、陰影排除與 flood fill（floorContextMs）
  ▼
地板與陰影判斷完成
  │ 10.61 ms  close／open 形態學與輪廓搜尋（morphologyContoursMs）
  ▼
候選輪廓建立完成
  │ 23.43 ms  中心線、幾何與連續性評分（candidateMs）
  ▼
最佳路徑選出
  │  0.01 ms  輪廓與 OpenCV 暫存資源收尾（cleanupMs）
  ▼
OpenCV 偵測完成（全管線 mean 52.48 ms）
  │  4.80 ms  detector callback 進入 Android UI 執行緒（visionToCallbackMs）
  ▼
UI 儲存最新辨識結果
  │  2.72 ms  等待控制更新（所有 newFrame=true 事件）
  ▼
循跡控制器開始計算
  │  0.21 ms  前置判斷、控制器決策與命令更新
  ▼
Virtual Stick 命令已更新
  │ 11.42 ms  等待第一個 40 Hz sender frame
  ▼
sendVirtualStickAdvancedParam
  │  0.82 ms  DJI MSDK 同步呼叫執行時間
  ▼
MSDK 呼叫返回
```

各區段平均值來自不同取樣集合，不能相加成單一影格的精確端到端延遲。端到端分布依 `monotonicNanos` 排序，只納入 `newFrame=true` 且命令在下一個 `control` 覆寫前確實送出第一個 `virtual_stick` frame 的事件；延遲由相機 `frameNanos` 算到 Virtual Stick `sentAt`，再加上 `sendCallMs`。

### 相機影格擷取至 MSDK 返回

| 指標 | 2026-08-31 基準 | 2026-09-01 14:21 | 2026-09-01 16:04 最佳化後 | 較 14:21 改善 |
|---|---:|---:|---:|---:|
| 樣本 | 322 | 637 | **312** | — |
| mean | 112.21 ms | 74.27 ms | **70.69 ms** | **4.8%** |
| P50 | 99.61 ms | 71.39 ms | **68.22 ms** | **4.4%** |
| P95 | 181.63 ms | 112.78 ms | **104.34 ms** | **7.5%** |
| P99 | 197.76 ms | 128.51 ms | **110.58 ms** | **14.0%** |
| max | 1,054.48 ms | 133.89 ms | **193.21 ms** | **退步 44.3%** |

最佳化後的 mean、P50、P95 與 P99 均改善；max 則被單一影格離群拉高。最新量測窗較短，不能以樣本數宣稱整體穩定度勝過 14:21 的 102.406 秒實驗。

### OpenCV 耗時

量測窗內共處理 **451 個影格**，有效處理率約 **12.52 fps**：

| 區段 | mean | P50 | P95 | max |
|---|---:|---:|---:|---:|
| 複製與排隊 `queueMs` | 1.22 ms | 1.04 ms | 2.25 ms | 5.04 ms |
| 前處理 `preprocessMs` | 12.87 ms | 13.70 ms | 19.18 ms | 32.94 ms |
| 黑色分割 `thresholdMs` | 0.92 ms | 0.80 ms | 1.42 ms | 9.13 ms |
| 地板與陰影 `floorContextMs` | 3.38 ms | 2.98 ms | 5.34 ms | 50.04 ms |
| 形態學與輪廓 `morphologyContoursMs` | **10.61 ms** | **10.36 ms** | **14.12 ms** | **17.01 ms** |
| 候選路徑評分 `candidateMs` | **23.43 ms** | **18.69 ms** | **54.02 ms** | **104.77 ms** |
| 收尾 `cleanupMs` | 0.01 ms | 0.01 ms | 0.02 ms | 0.28 ms |
| 其他 `otherMs` | 0.03 ms | 0.03 ms | 0.06 ms | 0.36 ms |
| **全管線 `totalMs`** | **52.48 ms** | **48.40 ms** | **84.23 ms** | **130.67 ms** |

相較 14:21 實驗，全管線 mean **54.84→52.48 ms**、P95 **90.41→84.23 ms**，分別改善 **4.3%**、**6.8%**；max **127.76→130.67 ms**，退步 **2.3%**。`candidateMs` 仍是主要計算成本，其 mean 與 P95 改善，但單一最大值從 88.24 ms 升至 **104.77 ms**。

### 偵測後的控制耗時

| 區段 | 樣本 | mean | P50 | P95 | max |
|---|---:|---:|---:|---:|---:|
| 偵測完成至 UI callback | 451 | 4.80 ms | 4.60 ms | 6.24 ms | 30.14 ms |
| UI 至控制，所有新影格 | 452 | **2.72 ms** | **2.03 ms** | **7.21 ms** | **31.63 ms** |
| 前置判斷 `preDecisionMs` | 1,150 | 0.03 ms | 0.02 ms | 0.05 ms | 0.45 ms |
| 控制器決策 `decisionMs` | 1,150 | 0.14 ms | 0.10 ms | 0.31 ms | 3.00 ms |
| 決策至命令更新 `decisionToCommandMs` | 1,150 | 0.04 ms | 0.03 ms | 0.08 ms | 1.15 ms |
| 命令至第一個 Virtual Stick frame | 312 | **11.42 ms** | **10.38 ms** | **23.13 ms** | **49.17 ms** |
| MSDK 同步呼叫 | 312 | **0.82 ms** | **0.63 ms** | **1.83 ms** | **5.28 ms** |

`uiToTickMs` 是本次弧長最佳化直接影響的區段。相較 14:21，mean **18.14→2.72 ms**、P50 **11.86→2.03 ms**、P95 **50.53→7.21 ms**、max **92.60→31.63 ms**，改善幅度分別為 **85.0%**、**82.9%**、**85.7%**、**65.8%**。這證明累積弧長表與二分搜尋已消除 UI 執行緒內的重複平方級路徑掃描。

量測窗內 **1,437／1,437** 個 Virtual Stick frame 全部成功，Flight Log 持續回報實際發送率約 **40.000 Hz**。

### 離群事件、斷流與飛行結果

最新最大端到端延遲為 sequence 874 的 **193.21 ms**。同一影格的 OpenCV `candidateMs` 為 **104.77 ms**、`totalMs` 為 **130.67 ms**，detector callback 為 **30.14 ms**，`uiToTickMs` 為 **8.54 ms**。因此剩餘長尾主要來自 OpenCV 候選評分與偶發 UI callback，而不是已最佳化的中心線弧長查詢。

量測窗內辨識結果為 `detected=true` **419 次**、`detected=false` **32 次**，辨識率 **92.9%**；一般連續未辨識最長 **7 幀／358 ms**。另有一次相機串流中斷 **1,603.8 ms**，控制器進入 `RECOVERING_FRAME_STREAM`，期間 **30 次控制輸出全部為零**，串流恢復後自動回到 `TRACKING`。

控制器命令速度 mean **1.001 m/s**、P95 **1.336 m/s**、max **1.347 m/s**，確實逼近 **1.35 m/s** 設定；DJI 地速回報則為 mean **0.404 m/s**、P95 **0.781 m/s**、max **0.806 m/s**，因此本次不能宣稱實際機體達到 1.35 m/s。

`diagnostic_turn_cycle` 的兩筆週期為 16.31 秒與 13.58 秒，但分別累積約 636°、876°，且皆標記 `notPhysicalLap=true`，不能當作實體圈速。循跡最後由畫面右搖桿接管：Flight Log 在 `16:05:29.200` 記錄停止，`16:05:29.201` 記錄 `stick engaged RIGHT`。

## 2026-08-31 軟體延遲基準

資料來源為 2026-08-31 14° 定向提速實驗的 `flight-profile-20260831-latest.tsv`。量測窗從 `tracking_start` 到 `tracking_stop`，共 **34.009 秒**；循跡模式為固定航向、目標速度 **1.25 m/s**、控制相位超前 **14°**。

```text
相機影格擷取
  │  1.10 ms  複製 RGBA 影格並排入 detector worker（queueMs）
  ▼
Detector worker 開始處理
  │ 13.45 ms  Mat、縮放、RGBA→RGB／灰階與模糊（preprocessMs）
  ▼
影像前處理完成
  │  0.88 ms  Otsu 門檻、黑色分割與明暗修正（thresholdMs）
  ▼
黑色分割完成
  │  3.31 ms  Lab 地板顏色、陰影排除與 flood fill（floorContextMs）
  ▼
地板與陰影判斷完成
  │  9.93 ms  close／open 形態學與輪廓搜尋（morphologyContoursMs）
  ▼
候選輪廓建立完成
  │ 32.98 ms  中心線、幾何與連續性評分（candidateMs）
  ▼
最佳路徑選出
  │  0.01 ms  輪廓與 OpenCV 暫存資源收尾（cleanupMs）
  ▼
OpenCV 偵測完成（全管線 mean 61.68 ms）
  │  5.03 ms  detector callback 進入 Android UI 執行緒（visionToCallbackMs）
  ▼
UI 儲存最新辨識結果
  │ 21.38 ms  等待控制更新（所有 newFrame=true 事件）
  ▼
循跡控制器開始計算
  │  0.14 ms  前置判斷、控制器決策與命令更新
  ▼
Virtual Stick 命令已更新
  │ 21.04 ms  等待第一個 20 Hz sender frame
  ▼
sendVirtualStickAdvancedParam
  │  0.93 ms  DJI MSDK 同步呼叫執行時間
  ▼
MSDK 呼叫返回
```

相機影格擷取至 MSDK 呼叫返回的逐幀實測分布：

- 樣本：**322** 個有新影格且在下一次命令覆寫前實際送出的控制命令
- mean **112.21 ms**
- P50 **99.61 ms**
- P95 **181.63 ms**
- P99 **197.76 ms**
- max **1,054.48 ms**

各區段平均值來自不同取樣集合，不能直接相加成同一影格的精確端到端結果；逐幀分布以實際命令和第一個對應 Virtual Stick frame 的時間戳關聯計算。

## OpenCV 耗時

循跡窗內共處理 **411 個影格**，有效處理率 **12.09 fps**：

| 區段 | mean | P50 | P95 | max | 佔全管線 mean |
|---|---:|---:|---:|---:|---:|
| 複製與排隊 `queueMs` | 1.10 ms | 0.91 ms | 1.99 ms | 5.30 ms | 管線外 |
| 前處理 `preprocessMs` | 13.45 ms | 13.86 ms | 18.28 ms | 26.61 ms | 21.8% |
| 黑色分割 `thresholdMs` | 0.88 ms | 0.72 ms | 1.93 ms | 8.60 ms | 1.4% |
| 地板與陰影 `floorContextMs` | 3.31 ms | 2.94 ms | 4.94 ms | 41.56 ms | 5.4% |
| 形態學與輪廓 `morphologyContoursMs` | 9.93 ms | 9.49 ms | 12.68 ms | 21.78 ms | 16.1% |
| 候選路徑評分 `candidateMs` | **32.98 ms** | **23.78 ms** | **71.55 ms** | **552.47 ms** | **53.5%** |
| 收尾 `cleanupMs` | 0.01 ms | 0.01 ms | 0.03 ms | 0.08 ms | <0.1% |
| 其他 `otherMs` | 0.03 ms | 0.02 ms | 0.08 ms | 0.40 ms | <0.1% |
| **全管線 `totalMs`** | **61.68 ms** | **52.65 ms** | **101.04 ms** | **577.34 ms** | 100% |

最大計算瓶頸是 `candidateMs`，平均佔全管線 53.5%。它包含逐候選遮罩建立與複製、中心線抽取、幾何／拓撲品質判斷及顏色脈絡評分。影格 sequence 1189 的 `candidateMs` 達 552.47 ms，是本次全管線最大值的主要來源；排除兩個超過 100 ms 的影格後，mean 仍為 31.50 ms，因此 32.98 ms 不只是單次離群值造成。P50 23.78 ms 代表一般影格，但候選評分的整體成本與長尾都必須優先改善。

## 偵測後的控制耗時

| 區段 | 樣本 | mean | P50 | P95 | max |
|---|---:|---:|---:|---:|---:|
| 偵測完成至 UI callback | 411 | 5.03 ms | 4.78 ms | 7.09 ms | 15.91 ms |
| UI 至控制，所有新影格 | 412 | **21.38 ms** | **13.72 ms** | **47.40 ms** | **429.93 ms** |
| 前置判斷 `preDecisionMs` | 645 | 0.02 ms | 0.02 ms | 0.05 ms | 0.12 ms |
| 控制器決策 `decisionMs` | 645 | 0.09 ms | 0.07 ms | 0.20 ms | 0.39 ms |
| 決策至命令更新 `decisionToCommandMs` | 645 | 0.03 ms | 0.02 ms | 0.06 ms | 0.66 ms |
| 命令至第一個 Virtual Stick frame | 322 | **21.04 ms** | 18.70 ms | **45.98 ms** | 50.38 ms |
| MSDK 同步呼叫 | 322 | 0.93 ms | 0.65 ms | 2.72 ms | 4.84 ms |

412 次 `newFrame=true` 控制中，324 次由 `FRESH_VISION` 執行，88 次由已先到期的 `PERIODIC` 執行；控制樣本比窗內完成處理的 411 個影格多一筆，是因循跡開始後先消化了一筆開始前已進入 UI 的辨識結果。這張表使用全部新影格事件，反映實際執行行為，不以 `FRESH_VISION` 子集合取代整體結果。

控制器計算本身不是瓶頸：前置判斷、決策與命令更新平均合計約 **0.14 ms**。主要等待仍在 UI 至控制的排程，以及命令至下一個 20 Hz Virtual Stick frame。

循跡窗內 **680／680** 個 Virtual Stick frame 全部成功，實測發送率 **19.995 Hz**。

## 2026-08-31 離群事件

2026-08-31 最大延遲集中在同一個影格 sequence 1189，而不是三個互不相關的最大值：

1. `candidateMs` **552.47 ms**，使 `totalMs` 達 **577.34 ms**。
2. 同一影格進入 UI 後，由 `PERIODIC` 控制處理，`uiToTickMs` 達 **429.93 ms**。
3. 該影格對應的相機擷取至 MSDK 返回總延遲達 **1,054.48 ms**。

因此以 2026-08-31 完整 profiling 而言，最重要的延遲風險是候選評分偶發長尾與控制排程長尾疊加。P95 仍為 181.63 ms，但單次最大值已超過 1 秒，後續優化不能只看平均值。

## 硬體反應量測狀態

2026-08-31 的 14° 定向提速實驗持續改變水平速度命令，沒有獨立的階躍輸入，因此無法從這份 trace 分離 RC、飛控、機體與速度遙測的反應時間。本報告不沿用其他實驗的舊硬體延遲數字；若要更新硬體側結果，必須以目前版本重新執行固定幅度的命令脈衝實驗。

## 滑冰模式實體圈速

### 2026-09-01 14:21 前一版實飛：1.35 m/s、16°

以 `/tmp/screen-20260901-145550.mp4` 的第 19–120 秒為分析窗，先降採樣至 5 fps，再排除固定 UI，並以不同畫面 ROI 交叉檢查相同構圖重現週期。代表性相似構圖時間點如下：

```text
20.4、30.0、38.8、47.4、55.4、65.4、75.0、84.4、92.6、101.4、110.4、119.2 秒
```

| 圈間隔 | 秒 |
|---:|---:|
| 1 | 9.6 |
| 2 | 8.8 |
| 3 | 8.6 |
| 4 | 8.0 |
| 5 | 10.0 |
| 6 | 9.6 |
| 7 | 9.4 |
| 8 | 8.2 |
| 9 | 8.8 |
| 10 | 9.0 |
| 11 | 8.8 |
| **合計／平均／中位數** | **98.8／8.98／8.8 秒** |

因此 14:21 實驗可報告為：**影片相同構圖重現週期估算的實體圈速平均約 8.98 秒／圈，中位數 8.8 秒／圈**。這比控制器角度回繞更接近實際飛行，但仍不是光閘或固定地標事件計時；畫面透視、構圖相似度與 5 fps 取樣會造成約 0.2 秒級量化誤差，不能宣稱為競賽級精確圈速。

`diagnostic_turn_cycle` 只統計控制器內部方向或速度里程的診斷週期，事件已明確標記 `notPhysicalLap=true`。本報告不再用它推導、替代或比較實體圈速。

## 結論

1. 弧長最佳化後，端到端延遲為 mean **70.69 ms**、P50 **68.22 ms**、P95 **104.34 ms**、P99 **110.58 ms**、max **193.21 ms**。相較 14:21 實驗，一般分布改善，但單一疊加離群使 max 退步。
2. `uiToTickMs` mean **18.14→2.72 ms**、P95 **50.53→7.21 ms**，分別改善 **85.0%**、**85.7%**；累積弧長表與二分搜尋已消除本次鎖定的重複路徑掃描成本。
3. OpenCV 全管線為 mean **52.48 ms**、P95 **84.23 ms**、max **130.67 ms**；`candidateMs` 仍是下一個主要計算成本與長尾來源。
4. 命令至第一個 40 Hz Virtual Stick frame 為 mean **11.42 ms**、P95 **23.13 ms**；MSDK 呼叫 mean **0.82 ms**。量測窗內 **1,437／1,437** 個 Virtual Stick frame 全部成功。
5. 實飛循跡 **36.009 秒**。一次 **1,603.8 ms** 相機斷流期間，30 次控制命令全部歸零並在恢復後續飛；最後由操作者以畫面右搖桿接管停止。
6. 控制命令最高達 **1.347 m/s**，但 DJI 地速回報 max 僅 **0.806 m/s**，本次不能宣稱實際機體達到 1.35 m/s。16:04 實驗未重新估算物理圈速；前一版 14:21 錄影的 **8.98 秒／圈**結果保留為歷史影片估算。